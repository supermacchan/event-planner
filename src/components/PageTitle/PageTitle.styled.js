import styled from 'styled-components';

export const Title = styled.h2`
    color: var(--dark-gray);
    font-size: 24px;
    font-weight: 600;
    line-height: normal;

    @media screen and (min-width: 768px) {
        font-size: 32px;
    }
`